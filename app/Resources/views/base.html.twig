<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
        <ul>
            <li>
                <a href="{{ path('batterypack_new') }}">Create a new batteryPack</a>
            </li>
            <li>
                <a href="{{ path('batterypack_index') }}">List all Batteries deposit</a>
            </li>
            <li>
                <a href="{{ path('homepage') }}">index</a>
            </li>
        </ul>
    </body>
</html>
